# ─────────────────────────────────────────────────────────────
# 第三方参考仓库：只读用，不入库
#   · 它自带 .git，是别人的仓库（556M），提交会变成"幽灵子模块"
#   · 框架版本由 my-agent/pyproject.toml 的 openai-agents>=0.22.3 锁定
#   · 换机器时重新 clone 即可，不需要 vendor 进来
# ─────────────────────────────────────────────────────────────
openai-agents-python/

# ─────────────────────────────────────────────────────────────
# Langfuse 源码（B 方案：fork + i18n + 自建镜像）
#   · 自带 .git，是上游仓库的克隆，提交会变成"幽灵子模块"
#   · 含 node_modules(2.5G) + .next(2.8G)，共 5.7G
#   · 换机器重新 git clone --depth=1 即可，不需要入库
# ─────────────────────────────────────────────────────────────
tools/langfuse-src/

# macOS / 编辑器
.DS_Store
.vscode/

# 运行产物与缓存
.tmp/
.cache/
*.log

# 凭据（子目录里各自也有一份 .gitignore，这里是兜底）
config.env
*.env
!*.env.example
