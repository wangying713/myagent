"""工具层：模型能"做"的事情全在这里。

── 一条纪律，防止越写越乱 ──────────────────────────────
一个业务域一个文件（repo.py / order.py / user.py ...），
文件里的每个工具只做"薄转换"：

    取值 → 校验与护栏 → 调真正的业务逻辑 → 裁剪成模型能读的文本

真正的业务逻辑（查库、调 RPC、算规则）**不写在这里**，放 services/。
这样它可以脱离 LLM 单独测试，也可以在别处复用。
这个分法跟你熟悉的后端分层是一回事：工具 = handler，services = service。
"""

from .repo import REPO_TOOLS

__all__ = ["REPO_TOOLS"]
